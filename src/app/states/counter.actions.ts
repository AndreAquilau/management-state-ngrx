import { createAction } from '@ngrx/store';

export const increment = createAction('[Component Boot] Increment');
export const decrement = createAction('[Component Boot] Decrement');
export const reset = createAction('[Component Boot] Reset');
